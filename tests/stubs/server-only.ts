// Empty stub aliased in for the `server-only` package during tests. The real
// package only guards against importing server modules into client bundles,
// which is irrelevant under Vitest's Node environment.
export {};
